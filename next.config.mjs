/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "qgttagrgdkvqhu2l.public.blob.vercel-storage.com",
      },
    ],
  },
};

export default nextConfig;
